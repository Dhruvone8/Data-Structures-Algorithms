/*
 * Problem: 802. Find Eventual Safe States
 * Difficulty: Medium
 * Date: 1/11/2026
 */

class Solution {
public:
    bool dfs(int vertex, vector<int>& visited, vector<int>& pathVisited,
             vector<int>& ans, vector<vector<int>>& graph) {
        // Mark the node as visited
        visited[vertex] = 1;

        // Mark the node as path visited
        pathVisited[vertex] = 1;

        // Traverse the adjancency list
        for (auto& vec : graph[vertex]) {
            // If its not visited
            if (!visited[vec]) {
                // If cycle exists
                if (dfs(vec, visited, pathVisited, ans, graph) == true)
                    return true;
